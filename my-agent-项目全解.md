# 我的项目：论文调研 Agent（提问主线版全解）

> 本文用一个例子走完全项目：**你问了一句"帮我调研 LangGraph 和 LangChain 的区别"**。
> 每走一步，讲清楚这一步在干什么、谁在干活（哪个文件）、参数怎么设计的（数字 + 为什么）。
> 一个文件讲完整个项目，不用看别的文件。

---

## 开场：项目定位与技术栈

**一句话定位**：一个论文调研 Agent——你提问，它自主判断意图、先查本地记忆和历史调研产物，够就直接带引用回答；不够就自主启动深度调研（联网检索 PubMed / arXiv / 本地 PDF，问/答/写/审多角色协作补齐证据），最终带引用回答；全文获取走"免费文本接口优先 + 白名单下载 + 人工审批"的合规链路。

**核心哲学**：状态本地，能力外部——记忆、审计、任务、审批、检查点全在本地 SQLite；大模型、检索、全文获取借用外部能力。本地管账，外部借力。

**技术栈**：FastAPI（接口 + SSE 推送）、LangGraph（状态图编排 + SQLite 检查点 + 节点重试）、SQLite（记忆/审计/任务/审批/检查点）、litellm（大模型统一入口，默认 DeepSeek）、rank-bm25 / sentence-transformers（混合检索，向量缺失自动降级哈希）、pypdf（本地 PDF）。

**核心能力五条**（后面全篇围绕它们展开）：
1. **自主决策**——意图路由（判断该干啥）、证据门控（判断够不够）、升级调研（不够就自己补）
2. **多 Agent 协同**——调研子图里问/答/写/审四个角色分工，专家自主拆词、检索、有据回答、问够即止
3. **合规全文获取**——能拿文字走文本接口，要下载走白名单 + 人工审批，拿不到只给链接
4. **可靠生产设施**——幂等防重发、熔断防拖垮、审计可追溯、检查点可恢复
5. **量化评测闭环**——公开数据集评测，每次架构迭代跑原评测不退化

---

# 第一部分：主线——你问了一句"帮我调研 LangGraph 和 LangChain 的区别"

## 第 1 步：你提问，消息进系统（入口）

1. **消息检查**——消息不能为空，空消息直接拒绝
2. **识别你是谁、哪场对话**——请求带上线程号（thread_id，同一场对话共用一个，不填自动生成）和请求号（request_id，这一次请求的唯一编号）
3. **决定怎么处理**——是走对话问答（一次问答），还是走调研任务（产出一篇报告）

**谁在干活**：`api.py`——项目唯一对外入口，共 11 个接口。

**参数设计（大白话）**：
- `thread_id` 为什么要有：检查点按线程号存档、幂等按线程号分账——**没有它，断点续跑和防重发不知道归到哪场对话**。
- `request_id` 为什么要有：**幂等去重**——同一个请求号再发一次，直接返回上次结果。前端双击、断线重连都会重发请求，没有它就会重复触发调研、重复花钱。
- `run_mode`：`research` 真实模式（调大模型、真检索）；`fake` 离线演示模式（不花钱跑通流程）。
- `allow_deep_research`：证据不够时允不允许系统自己升级调研——默认允许，这是"自主性"的开关。
- 调研任务的入参：主题（必填）、检索源（默认 pubmed）、视角数（默认 1）、问答轮数（默认 1）。

**拿到：消息已接住 + 这场对话的"身份"（线程号 + 请求号）。**

## 第 2 步：过两道闸（生产控制面）

1. **幂等查重**——同一个请求号重发，直接返回上次结果，不重复执行、不重复花钱
2. **熔断检查**——上游大模型连续失败过，先熔断别硬打

然后开一个"追踪区间"（span），后续每一步记耗时和结果，最后落审计。

**谁在干活**：`research_production.py`——治理闸本体 + 生产运行时（最大的两个类都在这里）。

**参数设计（大白话）**：
- **幂等**：请求号 + 全量参数算一个"内容指纹"（哈希）存进幂等表；同号再来直接返回上次结果。超时 30 秒。
- **熔断**：按操作计数，连续失败 **3 次**打开熔断、**冷却 30 秒**；冷却期不打下游、直接返回兜底结果；冷却后试探一次，成功闭合。**为什么**：接口抖动时硬打只会更糟，先歇会儿。
- **审计**：谁、何时、对什么资源、做了什么、允许还是拒绝——无条件写审计表。**为什么**：每个动作可追溯，这是产品的可信度卖点。
- **追踪**：每个节点跑完记一条记录（耗时/状态/参数），父子串成完整链路，按追踪号可查每一步花了多久。
- **权限**：单用户模式授权直接放行但留审计；多租户权限校验代码保留，切回即用。

**拿到：放行凭证 + 全程记账的追踪号。**

【强调】**幂等、熔断、审计、追踪是"可靠性"不是"权限"**——单用户也全保留，因为每个设施都对应一个真实会发生的故障场景（双击重发、接口抖动、出了事要追责）。

## 第 3 步：判断你想干嘛（意图路由）

两层判断：
1. **规则兜底**（不花钱、离线可跑）：关键词判断——"调研/论文/文献"→ 要调研，"区别/是什么"→ 知识问题要检索，"你是谁/怎么用"→ 系统帮助，短句追问 → 结合上下文重写
2. **大模型增强**：再让 LLM 判断一次，返回意图、工具、置信度

**采纳规则**：LLM 判断要有 **65%** 以上把握**并且**不和规则冲突才采纳；失败/超时自动回退规则。

**谁在干活**：`research_intent_router.py`（判定逻辑）+ `research_router_llm.py`（大模型调用工厂）。

**参数设计（大白话）**：
- 规则产出：意图、用哪个工具、要不要检索、**重写后的干净搜索词**、置信度、理由。规则之间互斥保护（含"调研"就不判成"系统帮助"）。
- 大模型参数：温度 0（不发挥）、最多 180 个 token、超时 20 秒、**512 条缓存**（同样的问题同样上下文直接复用上次判定，不重复花钱）。
- 同文件里另两个大模型工厂：**闲聊/生成**——温度 0.7（有发挥空间）、400 token、25 秒超时；**证据裁判**——温度 0、30 token（只许回三个词）、20 秒超时。**为什么温度不一样**：判断类的事要稳定（0），生成内容要有发挥（0.7）。离线/演示模式默认不调付费接口。

本例："帮我调研 LangGraph 和 LangChain 的区别"含"调研"+"区别" → 判定为**调研/检索类问题**。

**拿到：意图 + 工具 + 重写后的干净搜索词（"LangGraph LangChain 区别"）。**

## 第 4 步：翻自己的记忆（记忆召回）

去 SQLite 长期记忆找相关背景：按用户命名空间隔离 → 混合召回（字面 + 语义向量 + 实体 + 时效 + 近期度，排名融合去重）→ 取最多 **5 条**。**不联网**。

**谁在干活**：`research_longterm_memory.py`。

**参数设计（大白话）**——这里把"写入"参数也一起讲了（对话结束会用到）：
- **三档置信**：你明确说"请记住"的事 0.96；你的偏好、程序性规则 0.88；试探性内容 0.70。
- **分流**：置信度 **0.85 以上直接入库；0.65~0.85 进待定区**（以后再说一次就转正）；低于 0.65 丢弃。
- **版本化**：同一条事实内容改了 → 旧记录标"被取代"+写失效时间+指向新记录——**不删旧数据，任何时间点都能查**。**为什么**：用户可能反悔（之前说用中文、后来改英文），直接覆盖就丢了历史。
- **召回五信号**：字面 + 向量 + 实体 → 排名合并 → 时间加权（30 天衰减一半）→ 去重（阈值 0.2）→ 前 5 条。

本例：没聊过相关话题 → 记忆为空。

**拿到：最多 5 条相关背景（本例为空）。**

## 第 5 步：三路分支——知识问题走检索

**先讲这张图**：到这里主编排图开始"分岔"。`research_langgraph.py` 是整场对话的路线图——**10 个节点 + 3 个岔路口**，状态里流转：消息、话题、意图判定、记忆命中、知识检索结果、证据判定、调研结果、答案、引用、依据标记、追踪号等。

第一个岔路口按意图分三路：闲聊 → 直接聊；记忆类 → 拿记忆答；知识类（本例）→ 查知识库。

1. 查当前会话有没有做过的调研任务
2. 有 → 从任务产物建索引，混合检索取前 3 条
3. 没有 → 结果为空（本例如此）

**谁在干活**：`research_kb_qa.py`（知识库问答）+ `research_retrieval_index.py`（混合检索引擎）+ `research_retrieval_runtime.py` / `common.py`（检索运行时和公共件）。

**参数设计（大白话）**——混合检索四层：
1. **字面匹配（BM25）**：按词频算相似。英文保词组、中文拆单字+双字；提问套话（"请问/为什么"）先滤掉。
2. **语义向量**：算"意思像不像"——没出现关键词也能找到。默认 384 维多语言模型（本地跑）；没装模型自动降级 64 维哈希向量（零依赖）。
3. **排名合并（RRF）**：不看分数看排名——两个榜单按「1 ÷ (60 + 排名)」合并。
4. **相关度门槛**：实质词没重叠、向量相似度低于 **0.45** 的直接丢——**答不了的就不硬答**。重排（交叉编码器二次打分）质量最高但慢，默认关。

流程：**捞 20 条候选 → 合并排名 → 门槛过滤 → 取前 3 条**。

**拿到：空结果（没有现成知识库）。**

## 第 6 步：证据裁判——判够不够（自主决策点 1）

1. **规则裁判兜底**：完全没证据 → 直接判不够；裁判大模型失败时，证据 ≥ 2 条判够
2. **模型裁判**：把问题 + 前 5 条证据发给 LLM，只让回三个词——"可以回答 / 需要更多检索 / 无法回答"（温度 0、输出上限 30 token）
3. **一票判够**：模型说"可以回答"→ 判够；说"不够"→ 判不足

本例：检索为空 → 判"证据不足" → **自主升级深度调研**。

**拿到：判定 + 升级决定。**

【强调】**这是全项目最重要的自主行为**：系统自己发现"信息不够"，自己决定"要采取行动补足"——普通 RAG 拿到啥答啥，这里会自主启动一场调研。面试讲自主性，先讲这里。

## 第 7 步：深度调研——多角色循环（自主决策点 2 + 3）

提交调研任务，五个环节：

**A. 进入调研前，先讲"装配工"**（`research_graph_adapter.py`）：把三样东西装好——**检查点**（SQLite 断点续跑：每个节点跑完存一次快照到本地文件，连接长期持有、跨进程重启也能恢复；**为什么**：调研十几步跑几分钟，中间崩了从头再来太亏，重启后同线程号接着跑；连接失败降级进程内检查点）、**三源检索**、**全文回调 + skill 注入**。

**B. 检索器（三种数据源）**：
1. **PubMed**（`research_pubmed.py`）：三步取数——esearch（关键词搜文献编号）→ esummary（批量拿标题/期刊/日期/作者/**PMC 编号**）→ efetch（批量拿摘要）。每次取 **3 条**；请求间隔 **0.4 秒**（节流防限流）；没 API Key 也能用。**返回格式和另外两源统一**（链接、标题、摘要、片段、元数据），下游代码无感。
2. **arXiv**（`rm.py`）：调官方接口（按相关性排序、降序），拿标题、摘要、作者、分类、PDF 直链。
3. **本地 PDF**（`rm.py`）：扫指定目录，pypdf 提全文，滑窗切块（**1200 字符一块、重叠 150**），按词重合打分。

**C. 全文获取层（自主决策点 2——拿不到就自己想办法，但要合规）**（`research_fulltext.py`）：
系统对命中的文献，按优先级尝试拿全文：
1. **文本接口优先**（不产生文件、最干净）：有 PMC 编号 → PMC 文本接口直接返回全文；没有但有 PubMed 编号 → Europe PMC 文本接口拿全文文字。拿到返回前 800 字符预览。
2. **白名单下载（自动放行，不审批）**：下载链接来自权威源（PMC、Europe PMC、arXiv）→ 自动下载；仍做三重校验——内容类型必须 PDF、大小上限 **20MB**、下载后验文件头（**%PDF**）。
3. **非白名单下载（人工审批）**：不在可信域名清单内的文章下载 → 进人工审批队列（风险等级：白名单低、非白名单高）——SSE 推送"审批请求"→ 你批准/拒绝（普通 POST 回传）→ 批准才执行下载，拒绝则跳过。
4. **兜底**：都不行 → 只给原文链接，提示手动获取。

**审批队列的状态流转**：待审批 → 通过 → 下载中 → 已入库 / 下载失败；待审批 → 拒绝（只保留链接）。全程审计：谁批的、从哪下的、下成什么样。**审批只覆盖非白名单下载，白名单权威源自动放行——日常使用几乎无感。** 审批库与调研链路、治理面板共用同一个 SQLite 文件——调研中申请的审批，接口能查到能批，闭环可见。

**D. 多角色调研循环（核心大戏）——问、答、写、审四个角色**（`research_loop.py`）：
在 LangGraph 里建一个子图，七个节点对应四个角色：
1. **生成视角**——让模型想 1~3 个互补角度（机制/临床/安全这类），顺带输出一份"并行计划"（哪些子任务独立、建议并发数）。命中 skill 就把 skill 内容拼进提示词。模型输出坏了 → 回退默认（1 视角、并发 1）。
2. **作家 Agent（问）**——带视角提问、补漏不重复；觉得够了说"我没问题了"；装傻不结束就 **3 轮封顶**。
3. **专家 Agent（答）**——收到问题 → 自主拆成 **2~3 个搜索词**（拆失败用问题原文前 80 字符）→ 真去检索（逐词收集证据、截前 6 条）→ 基于检索资料有据回答、每句带 [编号]；**没搜到 → 明说"无法基于现有信息回答"，绝不硬编**；命中文献就触发全文获取（C 环节）。
4. **判断继续**——问够了去写作，没问够回去继续问。
5. **写作 Agent（写）**——把问答沉淀成文：先出提纲（章节+要点+引用编号）再写正文。**引用编号只能从真实检索结果池里取**——写作前从所有问答的证据构建引用池，模型编不了假引用。
6. **质检 Agent（审）**——检查三样：引用编号是否真实存在、有没有漏掉关键角度、有没有重复段落；产出四项评分卡（引用真实率/覆盖完整度/重复率/结构分）落盘；有问题打回写作重写（最多 **2 次**），合格才出稿。
7. **判断打回**——合格出稿 / 不合格打回重写 / 打回满了强制出稿。

流程一条线：**问 → 答 → 写 → 审**（问和答来回循环，审可打回写）。所有问答沉淀成信息表喂给写作。

**并行策略：模型自主调度 + 6 条硬边界**——不写死"哪些环节并行"，而是让模型根据任务结构自主决定：
- **模型怎么决定**：调研开始时（生成视角的同时），模型顺带输出一份"并行计划"——列出本次调研的独立子任务、各自是否独立、建议并发数。
- **硬边界（模型无权越过，系统写死）**：
  1. **并发上限 2~3**——模型建议 10 也建不出 10 个线程，防 API 限流、防成本失控
  2. **汇合点串行**——写作等所有问答完成、质检等全文完成，模型无权改动顺序（图结构保证）
  3. **循环内部串行**——单个问题内部（拆词→检索→回答）无并行入口
  4. **成本护栏**——开跑前估算：轮数 × 并发 × 每轮调用数 × 单次单价（**0.0002 美元**）；超过 **2 元**预警降并发、超过 **20 元**硬停
  5. **数据源限流**——PubMed 请求间隔 0.4 秒
  6. **无效计划回退**——模型输出格式错或建议离谱 → 回退默认（1 视角 + 并发 1）

一句话：**模型输出的是建议，代码校验后在边界内执行——"软的让模型想，硬的不让模型碰"，这就是受控自主的实现方式。**

**限制管控（防失控，两层）**：
- **通用管控（所有角色）**：① 次数上限——作家最多 3 轮、质检最多打回 2 次 ② 超时——每次调用超时按失败处理 ③ 预算——每个角色配 token 上限 ④ 重试——仅网络类错误重试（见下）
- **按角色红线**：作家——提问上限 + 必须带视角；专家——每问最多拆 3 词、必须基于检索结果回答（防幻觉最严一条）；写作——引用编号必须来自真实结果池（防编引用最严一条）；质检——检查项固定三样 + 打回必须带明确原因 + 上限 2 次

**节点重试（挂在主编排图上）**：两个联网节点（知识检索、深度调研）挂重试——只对网络类错误（连接失败、超时）重试，**最多 3 次尝试**、间隔 **1 秒起每次翻倍、封顶 30 秒**、带随机抖动。**为什么**：网络抖动是常态，重试是对外部故障的韧性；业务错误不重试。

**人工介入点（人在环，一处）**：非白名单下载审批（见 C）。其余环节全自动。**自主性和可控性并存：系统自主探索，人在关键点把关。**

**E. 写作 → 质检**：写作 Agent 两步干完（先提纲再正文）；质检 Agent 读全文检查（引用真实性 / 覆盖完整 / 重复段落），有问题打回重写，最多 2 次，合格出稿。**每次质检产出质量评分卡落盘**——引用真实率、覆盖完整度、重复率、结构分四项，任务之间可对比、版本迭代有量化依据。

**F. 产物落盘 + 知识回灌（闭环）**：成稿文章落盘（固定文件名）+ 调研结果 JSON（文章/提纲/评分卡/质检是否通过/引用池/视角清单/信息表）。**产物留在输出目录——之后对话问同类问题，直接用它建索引查询，不用重新调研（知识从"文章级"细化到"证据级"，调研越多越聪明）**。

**拿到：带引用的调研文章 + 更新的本地知识库。**

## 第 8 步：带引用回答 + 收尾

1. **组装回答**——优先用调研文章（引用来自真实引用池、有没有依据看引用池是否非空）；次选知识库答案；都没有 → 明确说"现有资料不足以回答该问题"。带"有没有依据"标记（grounded）、检索栈标识。
2. **写记忆**——按第 4 步讲的三档置信规则，把这次对话沉淀成长期记忆（本例"用户对 LangGraph 和 LangChain 的区别感兴趣"这类事实按 0.88 入库）。
3. **收尾**——追踪号、答案长度、引用数记进日志；整条链路 span 落账；如果是幂等重放（同请求号），直接读缓存结果。

**拿到：带引用答案 + 记忆更新 + 链路可追溯。**

【强调】**证据链**：每个论断 → 支撑它的证据块 → 来源文献 → 获取方式（PMC 文本 / 白名单下载 / 本地 PDF / 仅链接）→ 置信度——这是把 RAG 的可信度做到工程级，面试讲"可溯源"就讲它。

---

# 第二部分：核心模块详解（面试深挖点）

## 模块 1：全文获取层（合规获取，最值得讲的模块之一）

**为什么值得讲**：普通系统只拿摘要（综述级），这里能拿到方法、实验、数值（方法级）——全文是核心卖点，而"合规获取"是它的难点。

**获取四级方案（按优先级）**：
1. 文本接口：PMC / Europe PMC（直接返回文字，不产生文件，无下载风险）
2. 白名单下载（自动放行）：权威源 → 自动下载，不审批；仍做大小 / 类型 / 文件头三重校验
3. 非白名单下载（人工审批）：SSE 推"审批请求"→ 前端卡片 → 普通 POST 回传 → 批准才下载（状态机：待审批 → 通过/拒绝 → 下载中 → 已入库/失败）
4. 兜底：只给链接 + 提示手动获取

**关键设计点**：
- 下载不是主力，文本接口才是——能拿文字就不下文件
- 人工审批只覆盖"非白名单文章下载"；白名单权威源自动放行，日常几乎无感
- 审批走"人在环"：SSE 推请求 + 普通 POST 回结果，不需要 WebSocket
- 全程审计：谁批准、从哪下、下成什么样

## 模块 2：多角色调研循环（问/答/写/审）

**为什么这么设计**：四个角色对应四个动作——问、答、写、审，好记好讲。

**关系**：主线单向接力（问 → 写 → 审），问 ↔ 答循环，审 → 写打回迭代；**并行由模型自主调度**（见第 7 步 D 的 6 条硬边界）；汇合点必等。

**规模参数（人话）**：默认 1 个视角 × 1 轮 × 2 个搜索词 × 每词 3 条结果；上限可调大到 3×3×3×3。**默认省钱，深度手动调。**

**防幻觉三机制（面试必讲）**：
1. 拆词再检索——口语问题不能直接搜，模型先改写成搜索词组
2. 有据回答——每句话要有检索资料支撑
3. 搜不到就明说——绝不硬编，答不了就拒答

## 模块 3：PubMed 检索器

三步取数：esearch（关键词 → 文献编号列表）→ esummary/efetch（批量拿元数据 + 摘要）。返回格式与 arXiv/本地 PDF 统一（链接/标题/摘要/片段/来源类型），下游检索、证据裁判、引用组装全部无感。**PMC 编号在元数据里直接带上——全文获取层第一步就看它。** 请求间隔 0.4 秒节流，没 Key 也能用。

## 模块 4：质量评分卡（让质量可量化）

每次调研产出评分卡落盘，四项：引用真实率、覆盖完整度、重复率、结构分。任务之间可对比、版本迭代有量化依据——直接接上"评测闭环"叙事，面试有数据可讲。

## 模块 5：skill 领域增强

**skill 是什么**：一份 SKILL.md 说明书——文件头声明名称、描述、触发词；正文分节写领域术语、推荐视角、专用检索词、注意事项。**它不是代码，是"领域知识包"**，加载后注入调研流程，让 Agent 在特定领域调研得更专业。

**机制三步**：扫描（扫 skills 目录）→ 匹配（触发词规则先筛 + 大模型可选确认，只回 yes/no、失败保留规则结果；一次最多注入 **2 个**）→ 注入（拼进视角生成和专家回答的提示词——视角生成参考它的领域视角，专家回答遵循它的术语和注意事项）。

**和工具的关系（解耦）**：skill 管"怎么调研"（流程/知识/术语），工具管"能做什么"（检索/全文/问答）；不依赖 MCP。当前 skills 目录为空——机制完整，放个 SKILL.md 就生效。

## 模块 6：混合检索（四层底座）

**参数（大白话）**：
1. BM25 字面匹配——按词频算相似（中文拆单字+双字组合、英文保词组、滤提问套话）
2. 稠密向量语义匹配——算"意思像不像"（384 维本地模型，缺失降级 64 维哈希）
3. RRF 名次融合——不看分数看排名，按「1 ÷ (60 + 排名)」合并
4. 相关度门槛——实质词没重叠、向量相似度低于 0.45 的直接丢，答不了就拒答（可选交叉编码器重排，质量最高但慢，默认关）

流程：先捞 20 条候选 → 融合排名 → 门槛过滤 → 取前 3 条。

## 模块 7：长期记忆（自主提炼、版本化、召回）

**存储**：SQLite 四表——对话事件（防重）、事实（生效/失效时间 + 取代链）、待定候选、向量（按模型指纹隔离）。
**写入**：规则提炼四类事实 → 三档置信（0.96/0.88/0.70）→ 0.85 入库 / 0.65 待定转正 / 丢弃 → 冲突版本化不覆盖 → 全程留痕。
**召回**：五信号（字面/向量/实体 → 排名合并 → 时间加权 30 天衰减一半 → 去重阈值 0.2 → 前 5 条）。不联网。

## 模块 8：上下文引擎（长对话防爆窗）

**五层工作集**：固定提示（25% 预算）/ 活跃对话（35%）/ 摘要（20%）/ 记忆与证据（12%+35%）/ 产物（8%）。
**参数**：总预算 **4096** token、输出预留 **768**、软水位 **0.72**（触发压缩）、硬水位 **0.9**（强制压缩）、最近 **6 条**。
**压缩**：压前先还原旧摘要原文（防摘要压摘要误差累积）；摘要带血统链；原文存 SQLite 按压缩号 **100% 还原**。**为什么**：用户可能反悔，压缩掉的原文要能查回原话。
**谁在用**：会话层（`research_chat_agent.py` 管理的完整会话——建会话/发消息/压缩/还原/重新生成；已接出 HTTP 接口：`/api/sessions` 系列 8 个端点，2026-08-12 补上）。

## 模块 9：生产控制面（单用户为什么全保留）

**每个设施对应一个真实故障场景**（面试追问标准答法）：
1. **幂等（防重复执行）**——"前端网络抖动或用户点两下，同一个请求会重发。没有幂等，重试就会重复触发调研、重复花钱。我按请求编号登记，重发直接返回上次结果——重试安全，不重复花钱。"（真实场景：断线重连、双击提交）
2. **审计 + 证据链（可追溯）**——"我的产品卖点是'每个论断都有来源、每次下载都有记录'。审计不是给监管看的，是给用户看的——回答带证据链、下载带审批记录，这是可信度本身。"（把审计从"合规"转成"产品卖点"）
3. **记忆版本化（不覆盖只作废）**——"用户可能反悔——之前说'用中文'，后来改'用英文'。如果直接覆盖，历史就没了。作废+取代让任何时间点的记忆状态都能查。"（真实场景：用户改口）
4. **五层上下文 + 压缩（控制 token）**——"长会话会撑爆模型窗口，而且越聊越贵。我把上下文分层预算，用到 72% 就把旧消息压成摘要、原文存档——既不爆窗，又能 100% 还原。"（真实场景：聊 50 轮后模型忘前文）
5. **熔断 + 重试（对外部故障的韧性）**——"模型 API 或 PubMed 随时可能抖动/限流。熔断防止连续失败时硬打，重试只针对网络类错误、最多 3 次——外部故障不拖垮整个任务。"（真实场景：API 报错、限流）

## 模块 10：主编排图与断点续跑

**10 节点 + 3 岔路口**：意图分类 → 记忆召回 →（闲聊 | 记忆答 | 知识检索）→ 证据裁判 →（带引用回答 | 深度调研 | 拒答澄清）→ 记忆写入 → 收尾。
**SQLite 检查点**：每个节点跑完存快照，进程崩溃重启后同线程号接着跑——连接长期持有、跨进程可恢复、失败降级进程内。
**节点重试**：知识检索、深度调研两个联网节点——网络错误最多 3 次、1 秒起指数退避翻倍封顶 30 秒、带抖动。

---

# 第三部分：项目代码结构

```
my-agent/
├─ api.py                              # 接口层：11 个接口 + SSE
├─ start_research_service.py         # 启动脚本（含预检；实际启动走 uvicorn api:app）
├─ requirements.txt / setup.py / README.md   # 依赖 / 打包 / 说明
├─ research_agent/                    # 核心包（28 个文件）
│  ├─ research_service.py            # 服务层：任务生命周期 + 双入口调度 + 治理查询
│  ├─ research_production.py     # 治理层：幂等/熔断/审计/追踪/权限 + 生产运行时
│  ├─ research_graph_adapter.py  # 适配层：检查点 + 三源 + 全文 + skill 装配
│  ├─ research_langgraph.py      # 编排层：主编排图（10 节点 3 路由 + 重试）
│  ├─ research_intent_router.py      # 决策层：意图路由（规则 + 大模型）
│  ├─ research_router_llm.py         # 决策层：三个大模型工厂（路由/闲聊/裁判）
│  ├─ research_longterm_memory.py         # 数据层：长期记忆（五信号/三档置信/版本化）
│  ├─ research_context.py        # 数据层：五层上下文引擎（压缩/还原）
│  ├─ research_retrieval_index.py / runtime.py / common.py  # 检索层：混合检索引擎
│  ├─ research_kb_qa.py                 # 检索层：知识库问答（产物建库再查）
│  ├─ research_pubmed.py             # 检索层：PubMed 检索器（三步取数）
│  ├─ rm.py                            # 检索层：检索器集合（arXiv/本地 PDF 等）
│  ├─ research_fulltext.py           # 内容层：全文获取（文本接口/白名单/审批）
│  ├─ research_loop.py      # 内容层：多角色调研子图（问/答/写/审 + 6 条硬边界）
│  ├─ research_skill.py              # 内容层：skill 领域增强（扫描/匹配/注入）
│  ├─ research_qa.py        # 决策层：另一套证据裁判（规则打分 + 三词判定）
│  ├─ research_chat_agent.py         # 会话层：完整会话管理（压缩/还原等）
│  ├─ research_document_v41.py       # 工具层：PDF 提取与切块
│  ├─ research_tools.py              # 工具层：自研工具协议
│  ├─ research_memory.py             # 数据层：另一套记忆实现（会话层用）
│  ├─ lm.py                            # 兼容层：大模型抽象（litellm 统一网关 + 两层缓存）
│  ├─ utils.py                         # 工具层：加载 Key 等
│  └─ research_benchmarks.py / eval*.py / multi_task_benchmark.py  # 评测层
├─ evaluation/public_benchmarks/       # 评测层：公开数据集（SciFact/QASPER/LongMemEval/LongBench）
└─ skills/                             # skill 目录（当前为空，放 SKILL.md 即生效）
```

## 其他文件（不在主线，独立可用）

1. **`research_qa.py`（另一套证据裁判 Agent）**——规则打分（证据条数每条 +15 封顶 30、引用条数 +10 封顶 20、关键词重叠 +8 封顶 25、主题重叠 +5 封顶 15、期望关键词 +5 封顶 10、实质相关奖励 +20、排除词惩罚 -15；四道硬门槛）+ 大模型三词判定（判可以抬到 80 分、判不够压到 40 分）。独立入口 ask_research_agent；主编排图用的是更轻的内联裁判，两套并存。
2. **`research_document_v41.py`（PDF 处理）**——PDF 提取 + 三策略切块，独立复用件。
3. **`research_tools.py`（工具协议）**——自研"工具说明书"模式，预留注册层，主链未挂接。
4. **`research_memory.py`（另一套记忆）**——与记忆 v56 并存的一套实现，会话层使用。
5. **`lm.py`（大模型抽象）**——统一接口（`LitellmModel`：litellm 统一网关，支持 100+ provider）+ 两层缓存（内存最近 3000 条 + 磁盘跨进程缓存）+ 用量记账。纯自研实现，无 dspy 依赖。
6. **`utils.py`**——加载 Key 等小工具。
7. **评测体系**——自研运行器（seed 回归 / 消融对比 / 三场景评测）+ 公开数据集评测（SciFact / QASPER / LongMemEval / LongBench；指标：召回率 / MRR / nDCG@K / 置信区间）。**定位：验收标准——架构迭代后跑原评测不退化。**
8. **`start_research_service.py`**——带端口预检的启动脚本；当前实际启动走 `uvicorn api:app`。
9. **`requirements.txt` / `setup.py` / `README.md`**——依赖、打包、说明。

---

# 第四部分：工程细节定稿

## 4.1 检索与召回策略

**向量化（怎么把文字变成数字）**：
1. 真实向量：多语言向量模型（384 维），本地跑不联网
2. 哈希向量（降级）：纯数学哈希 64 维，零依赖——没装向量模型时自动退回
3. 向量持久化：写入时算好存 SQLite（按模型指纹隔离），查询只编码问题

**知识库召回（四级）**：BM25 字面 → 稠密向量语义 → RRF 名次融合 → 相关度门槛（0.45 / 实质词重叠）；流程 20 条候选 → 融合 → 过滤 → 前 3 条。

**记忆召回（五信号）**：字面 / 向量 / 实体 → 融合成主分 → 时效 + 近期度（30 天衰减一半）+ 重要度加权 → 去重 → 前 5 条。

## 4.2 接口清单（FastAPI + SSE，共 11 个）

- POST /api/chat —— 问答（message / thread_id / request_id / topic / run_mode / allow_deep_research）
- POST /api/kb/query —— 知识库查询（task_id / question / top_k 默认 3）
- POST /api/research —— 提交调研（topic / run_mode / retriever 默认 pubmed / 视角数 / 轮数）
- GET /api/research/{task_id} —— 查任务状态 / 产物
- GET /api/research/{task_id}/stream —— SSE 事件流（task_status / heartbeat）
- POST /api/research/{task_id}/approve —— 审批回传（approval_id / approve / reason）
- GET /api/approvals/pending —— 待审批列表
- GET /api/skills —— skill 列表
- GET /api/admin/status —— 治理面板：控制面健康
- GET /api/admin/audit —— 治理面板：审计留痕
- GET /api/admin/spans —— 治理面板：全链路追踪
- GET /api/health —— 健康检查

**SSE**：StreamingResponse + text/event-stream；每秒推状态、15 秒心跳、成功/失败断开；推送用 SSE、回传用普通 POST——不需要 WebSocket。

## 4.3 数据表结构（SQLite）

- **治理库**（WAL）：审计表、幂等表（请求号+指纹防重）、缓存表（值+过期+标签+命中数）、熔断表、任务队列表（幂等键防重入）、追踪表（按追踪号索引）、权限表
- **审批库**：审批项（审批号/任务号/链接/来源/大小/风险等级/状态/时间）
- **记忆库**：事件表（防重）、事实表（版本化）、候选表、向量表（模型指纹隔离）
- **检查点库**：LangGraph 每节点快照（按线程号）
- **上下文存档**（会话线）：事件表 + 压缩表（原文可还原）
- **任务与产物**（文件）：任务状态 JSON；产物目录——成稿、结果 JSON、全文下载、问答结果、评分卡、追踪日志

## 4.4 配置汇总（所有参数一处看完）

- **并行**：模型自主调度 + 6 条硬边界（并发 2~3 / 汇合点串行 / 循环内串行 / 成本 2 元预警 20 元硬顶 / PubMed 节流 0.4 秒 / 无效计划回退）
- **循环上限**：问-答 3 轮、质检打回 2 次
- **大模型**：路由——温度 0 / 180 token / 20 秒 / 512 缓存；闲聊——温度 0.7 / 400 token / 25 秒；裁判——温度 0 / 30 token / 20 秒
- **重试与熔断**：网络错误最多 3 次、1 秒指数退避翻倍封顶 30 秒、抖动；熔断 3 次 / 30 秒
- **检查点**：SQLite 持久化（自持连接），失败降级进程内
- **记忆**：前 5 条；置信 0.96 / 0.88 / 0.70；入库 0.85、转正 0.65；去重阈值 0.2
- **上下文**（会话线）：4096 / 768 / 0.72 / 0.9 / 最近 6 条
- **检索**：候选 20、取 3、公式 1/(60+排名)、门槛 0.45、重排默认关
- **全文**：白名单三项；20MB；PDF；%PDF 文件头
- **skill**：目录 skills/、最多注入 2 个
- **单用户**：放行留审计，多租户可切回

## 4.5 降级链（哪一环失败都不裸奔）

1. **全文拿不到** → 退回摘要级回答，回答明确标注"基于摘要"
2. **某个检索器失败**（如 PubMed 挂）→ 换另一个（arXiv / 本地 PDF）；全失败 → 明说"当前无法获取资料"，不硬编
3. **向量模型缺失** → 自动退哈希向量 / 纯 BM25
4. **模型调用失败** → 重试（网络类错误，最多 3 次）→ 仍失败走规则兜底（本地拼装回答 / 拒答）
5. **证据不足** → 拒答（"现有资料不足以回答该问题"）

## 4.6 安全

1. **引用池校验**：写作 Agent 产出的引用编号必须来自真实检索结果池（写死红线，防编引用）
2. **下载三重校验**：白名单 + 类型 / 大小 / 文件头
3. **非白名单下载人工审批**：人在环把关
4. **全程审计**：所有外部动作留痕（谁、何时、做了什么、结果）
5. **信息脱敏**：错误与配置中的密钥打码（sk- 前缀）

---

# 第五部分：设计主线（自主性叙事）与面试

## 五条设计主线

1. **自主决策闭环**：意图路由（判断该干啥）→ 证据门控（判断够不够）→ 升级调研（不够就自己动手补）——系统在关键决策点自主，不是固定流水线
2. **多 Agent 协同**：调研子图里多个角色分工，专家自主拆词、自主检索、有据回答、自主终止
3. **合规全文获取**：能拿文字走文本接口，要下载走白名单 + 人工审核，拿不到只给链接——"Agent 想拿什么就能拿什么"是能力，"怎么拿合规"是设计
4. **状态本地，能力外部**：记忆/审计/任务全在 SQLite；LLM、检索、全文获取走外部——本地管账，外部借力
5. **可恢复可重放可审计**：SQLite 检查点崩溃可恢复；幂等重放不重复花钱；全文获取和每步耗时都有审计

## 面试 30 秒话术

> "我自研了一个论文调研 Agent：LangGraph 状态图做编排（10 节点 + 3 条件路由），意图路由和证据门控让系统自主决策——证据不够就自主升级多角色调研（自研子图：问/答/写/审四个角色，专家自主拆词、检索、有据回答、问够即止，保留防幻觉机制）。数据源覆盖 PubMed、arXiv 和本地 PDF，全文获取走 PMC/EuropePMC 文本接口 + 白名单下载 + 人工审核队列，合规且可审计。SQLite 管记忆和幂等熔断，检查点崩溃可恢复，SSE 推进度。整个编排和调研引擎都是自己写的。"

## 面试追问准备

1. **单用户为什么保留生产设施**：见模块 9——每个设施对应一个真实故障场景，场景先行讲，面试官就信这是真做过，不是堆名词。
2. **并行是提示词还是硬编码**：分两层——并行计划由模型在提示词引导下生成（软的，自主决定"并哪些、并发多少"）；但 6 条边界全部硬编码（硬的，模型碰不到）——并发上限靠线程池写死、汇合点靠图结构、循环内串行根本没有并行入口、成本护栏和 PubMed 节流是纯代码逻辑、无效计划解析失败直接回退。**模型输出的是建议，代码校验后在边界内执行。**
3. **证据不足为什么敢自动升级调研**：这是自主性的核心卖点——不是固定流水线，系统在关键决策点自主判断、自主行动，成本有护栏（2 元/20 元）、深度有上限（3 轮 × 打回 2 次）、合规有审批（非白名单下载人在环）。

---

*（完。单文件全解，一个例子串全程，不依赖其他文档。）*
