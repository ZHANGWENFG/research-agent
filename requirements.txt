# ============================================================
# my-agent 运行时依赖（2026-08-15 清理）
# 原则: 只保留代码实际 import 的依赖；死依赖一律移除。
# 传递依赖（间接需要）显式固定并注明来源，避免 pip 解析漂移。
# ============================================================

# --- 模型层 ---
# 统一 LLM 网关（100+ provider，缓存/重试/成本统计内置）
litellm
# 真实嵌入模型（可选: 不装则自动降级 hash 嵌入，见 research_retrieval_runtime.py）
sentence-transformers>=3.4,<6.0
# 以下两个是 sentence-transformers 的传递依赖，显式固定防版本漂移
transformers>=4.45,<5.0
huggingface-hub>=0.34,<1.0
# 本地嵌入（QdrantVectorStoreManager 路径）
langchain-huggingface>=1.0,<2.0

# --- 编排层 ---
langgraph>=1.2,<2.0
langgraph-checkpoint-sqlite>=3.1,<4.0
# aiosqlite 是 langgraph-checkpoint-sqlite 的传递依赖
aiosqlite>=0.20,<1.0

# --- 检索层 ---
rank-bm25
qdrant-client
langchain-qdrant
numpy>=1.26,<3.0

# --- 文本处理 ---
langchain-text-splitters
trafilatura
pypdf
toml

# --- 服务层 ---
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
httpx>=0.27,<1.0
certifi>=2024.2.2
